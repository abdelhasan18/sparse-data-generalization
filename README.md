# Sparse Data Generalization
#### Abdelaziz Hasan
#### Christopher Gewont

---

This document is segmented into three stages, each accounting for their own dependencies and artefacts. All `.pdf` files are knit from their respective `.Rmd` files.

The following project files use the latest stable installation, OS specific, of `R` from: https://cran.rstudio.com

The latest stable installation, OS specific, of the RStudio IDE is also necessary, from: https://posit.co/download/rstudio-desktop

RStudio IDE simplifies knitting process with use of packages:

| Package | Documentation |
| --- | --- |
| `markdown` | https://cran.r-project.org/web/packages/rmarkdown/index.html |
| `knitr` | https://cran.r-project.org/web/packages/knitr/knitr.pdf |
| `tinytex` | https://yihui.org/tinytex/ |

For future reference, all package installations can be executed in the IDE's terminal using the following segment:
```{c}
for (item in dep) {
  if (!requireNamespace(item, quietly = TRUE)) {
    install.packages(item, dependencies = c("Depends", "Imports", "Suggests"))
  }
}
```
Where the user can inject their package name strings in the `dep` vector.

All files work with the UCI Dota2 dataset; descriptions of content as well as training and testing splits in `.csv` format can be found at: https://archive.ics.uci.edu/dataset/367/dota2+games+results

---

### Table of Contents

| Section | Contents |
| --- | --- |
| Analysis | Artefacts include `analysis.*` files. Used for preliminary discoveries regarding the data prior to applying any fitting techniques. |
| Decision Tree | Artefacts include `general_model.*` `overfitting_model.*` files. Used for main branch of experimentation. Operations are performed to fine-tune two models along unique criteria, as well chart visualizations for each step. |
| KNN | Artefacts include `knn_tuning.*` files. Operations are performed to fine-tune a model to observe the generalized effects beyond the constraints of a decision tree to test the application of our findings. Visualizations are charted at each step. |

---

## Analysis

#### Dependencies

All core functions used are part of `R`'s standard library, no external packages necessary.

#### Usage

- Load and preprocess data.
- View the distribution of classes.
- Applying principal component analysis.
- Visualizations depict the individual and cumulative proportions of variance explained by each principal component.

---

## Decision Tree

#### Dependencies

| Package | Documentation |
| --- | --- |
| `data.table` | https://cran.r-project.org/web/packages/data.table/index.html |
| `rpart` | https://cran.r-project.org/web/packages/rpart/index.html |
| `rpart.plot` | https://cran.r-project.org/web/packages/rpart.plot/index.html |
| `ggplot2` | https://cran.r-project.org/web/packages/ggplot2/index.html |
| `caret` | https://cran.r-project.org/web/packages/caret/index.html |

#### Usage

- Load and preprocess data using `fread(...)` from `data.table`.
- Rename fields and factorize the response variable using base `R`.
- Fit constrained and unpruned tree models using `rpart(...)` from `rpart`.
- Use `rpart.control(...)` from `rpart` to adjust `maxdepth`, `minsplit`, `minbucket`, and `cp`.
- Use `createFolds(...)` from `caret` to generate cross-validation splits for the general tree.
- Evaluate candidate tree settings across folds using `predict(...)` from `rpart`.
- Visualize cross-validation results using `ggplot(...)`, `geom_line()`, and `geom_point()` from            `ggplot2`.
- Print tree complexity summaries using `printcp(...)` from `rpart`.
- Visualize fitted trees using `rpart.plot(...)` from `rpart.plot`.
- Compute train and test validity metrics using `confusionMatrix(...)` from `caret`.

---

## KNN

#### Dependencies

| Package | Documentation |
| --- | --- |
| `class` | https://cran.r-project.org/web/packages/class/index.html |
| `caret` | https://cran.r-project.org/web/packages/caret/index.html |

#### Usage

- Load and preprocess data.
- Use `caret`'s `trainControl(...)` function to configure cross-validation parameters.
- Fit the models using `caret`'s `train(...)` function with parameter `method = "knn"`.
- Visualizations are created using `R`'s standard library using metrics provided in the object generated by the `train(...)` function.
- Individual model is fit using the `knn(...)` function from `class`.
- Train and test validity metrics are given via printing the `confusionMatrix(...)` object from `caret`.
